fun fib(a: int, b: int, n: int): int {
    if n == 0 {
        return 0;
    }

    print(a);
    fib(b, a+b, n-1);
}

fun main(): () {
    a: int = 0;
    b: int = 1;

    n: int = 4;

    fib(a, b, n);
}